import { ReactNode } from "react"

export type Props_layouts = {
    children: ReactNode
}
export type Props_note = {
    _id: string,
    title: string,
    description: string,
    createdAt: string
}