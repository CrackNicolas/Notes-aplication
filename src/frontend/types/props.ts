import { ReactNode } from "react"

export type Props_layouts = {
    children: ReactNode
}
export type Props_note = {
    id: string,
    title: string,
    description: string,
    creation_date: string
}